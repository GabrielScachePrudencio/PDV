<Window x:Class="PDV_LANCHES.Views.NovoUsuario"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cadastrar Novo Usuário" 
        WindowState="Maximized" 
        WindowStyle="SingleBorderWindow"
        WindowStartupLocation="CenterScreen" 
        Background="#020617">

    <Window.Resources>
        <Style x:Key="DarkComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" Cursor="Hand">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="30"/>
                                                </Grid.ColumnDefinitions>
                                                <Path Grid.Column="1" Data="M 0 0 L 4 4 L 8 0 Z" Fill="#94A3B8" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter Content="{TemplateBinding SelectionBoxItem}" Margin="{TemplateBinding Padding}" VerticalAlignment="Center" IsHitTestVisible="False"/>
                            <Popup IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Placement="Bottom">
                                <Border Background="#0F172A" BorderBrush="#334155" BorderThickness="1" CornerRadius="8" MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ItemsPresenter />
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Background" Value="#0F172A"/>
            <Setter Property="BorderBrush" Value="#1E293B"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#0F172A" BorderBrush="#1E293B" BorderThickness="0,0,1,0">
            <StackPanel Margin="0,40,0,0">
                <Border Width="60" Height="60" CornerRadius="30" Background="#3B82F6" HorizontalAlignment="Center">
                    <TextBlock Text="➕" FontSize="25" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="White"/>
                </Border>
                <TextBlock Text="NOVO CADASTRO" Foreground="White" FontSize="14" FontWeight="Black" Margin="0,15,0,0" HorizontalAlignment="Center"/>
                <TextBlock Text="Preencha os dados do funcionário" Foreground="#64748B" FontSize="11" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1" Margin="60,40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,40">
                <TextBlock Text="CONTROLE DE EQUIPE" FontSize="11" FontWeight="Black" Foreground="#3B82F6"/>
                <TextBlock Text="Criar Novo Usuário" FontSize="28" FontWeight="SemiBold" Foreground="White" Margin="0,5,0,0"/>
                <Separator Margin="0,15,0,0" Background="#1E293B"/>
            </StackPanel>

            <StackPanel Grid.Row="1" MaxWidth="800" HorizontalAlignment="Left">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="40"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="NOME COMPLETO" Foreground="#64748B" FontSize="10" FontWeight="Bold" Margin="0,0,0,8"/>
                        <TextBox x:Name="txtNome" Style="{StaticResource DarkTextBox}" Margin="0,0,0,20"/>

                        <TextBlock Text="E-MAIL" Foreground="#64748B" FontSize="10" FontWeight="Bold" Margin="0,0,0,8"/>
                        <TextBox x:Name="txtEmail" Style="{StaticResource DarkTextBox}" Margin="0,0,0,20"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="TIPO DE ACESSO" Foreground="#64748B" FontSize="10" FontWeight="Bold" Margin="0,0,0,8"/>
                        <ComboBox x:Name="cbTipo" Style="{StaticResource DarkComboBox}" Margin="0,0,0,20"/>

                        <TextBlock Text="DEFINIR SENHA" Foreground="#64748B" FontSize="10" FontWeight="Bold" Margin="0,0,0,8"/>
                        <PasswordBox x:Name="txtSenha" Height="45">
                            <PasswordBox.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="8"/>
                                    <Setter Property="Background" Value="#0F172A"/>
                                    <Setter Property="BorderBrush" Value="#1E293B"/>
                                </Style>
                            </PasswordBox.Resources>
                        </PasswordBox>
                    </StackPanel>
                </Grid>
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="CANCELAR" Width="150" Height="50" Background="Transparent" Foreground="#64748B" FontWeight="Bold" BorderBrush="#1E293B" BorderThickness="1" Margin="0,0,15,0" Click="Cancelar_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="10"/>
                        </Style>
                    </Button.Resources>
                </Button>
                <Button Content="CADASTRAR USUÁRIO" Width="220" Height="50" Background="#3B82F6" Foreground="White" FontWeight="Black" Click="Cadastrar_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="10"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</Window>