<UserControl x:Class="PDV_LANCHES.Views.ViewsAdministrativo.AlllCuponsDeDesconto"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">

    <Grid Margin="24">
        <Border Background="#0F172A" CornerRadius="16" BorderBrush="#1E293B" BorderThickness="1" Padding="24">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock Text="Gestão de Cupons" FontSize="24" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="Crie códigos de desconto para seus clientes." Foreground="#94A3B8" FontSize="13"/>
                </StackPanel>

                <Border Grid.Row="1" Background="#020617" CornerRadius="12" Padding="20" Margin="0,0,0,25" BorderBrush="#1E293B" BorderThickness="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="150"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="NOME DO CUPOM" Foreground="#64748B" FontSize="10" FontWeight="Bold" Margin="0,0,0,8"/>
                            <TextBox x:Name="txtNomeCupom" Height="40" Background="#0F172A" Foreground="White" BorderBrush="#1E293B" VerticalContentAlignment="Center" Padding="8"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="0,0,10,0">
                            <TextBlock Text="% (DESC)" Foreground="#64748B" FontSize="10" FontWeight="Bold" Margin="0,0,0,8"/>
                            <TextBox x:Name="txtPercentual" Height="40" Background="#0F172A" Foreground="White" BorderBrush="#1E293B" VerticalContentAlignment="Center" Padding="8"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2" Margin="0,0,10,0">
                            <TextBlock Text="R$ (FIXO)" Foreground="#64748B" FontSize="10" FontWeight="Bold" Margin="0,0,0,8"/>
                            <TextBox x:Name="txtValorFixo" Height="40" Background="#0F172A" Foreground="White" BorderBrush="#1E293B" VerticalContentAlignment="Center" Padding="8"/>
                        </StackPanel>

                        <StackPanel Grid.Column="3" Margin="0,0,10,0">
                            <TextBlock Text="VALIDADE" Foreground="#64748B" FontSize="10" FontWeight="Bold" Margin="0,0,0,8"/>
                            <DatePicker x:Name="dtValidade" Height="40" VerticalContentAlignment="Center"/>
                        </StackPanel>

                        <Button Grid.Column="4" Content="CRIAR CUPOM" Height="40" VerticalAlignment="Bottom" Background="#3B82F6" Foreground="White" FontWeight="Bold" Click="NovaCategoria_Click">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="8"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </Grid>
                </Border>

                <Border Grid.Row="2" CornerRadius="12" ClipToBounds="True" BorderBrush="#1E293B" BorderThickness="1">
                    <DataGrid x:Name="dgCupons" Background="#020617" AutoGenerateColumns="False" CanUserAddRows="False" RowHeight="55" BorderThickness="0" HeadersVisibility="Column" GridLinesVisibility="None">
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="#020617"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#0F172A"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="NOME" Binding="{Binding Nome}" Width="*"/>
                            <DataGridTextColumn Header="%" Binding="{Binding Percentual, StringFormat={}{0}%}" Width="100"/>
                            <DataGridTextColumn Header="VALOR" Binding="{Binding ValorFixo, StringFormat=C2}" Width="100"/>
                            <DataGridTextColumn Header="VALIDADE" Binding="{Binding DataValidade, StringFormat=dd/MM/yyyy}" Width="120"/>

                            <DataGridTemplateColumn Header="STATUS" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border x:Name="StatusBadge" CornerRadius="10" Padding="8,2" HorizontalAlignment="Left">
                                            <TextBlock x:Name="StatusText" FontSize="10" FontWeight="Bold" Foreground="White"/>
                                        </Border>
                                        <DataTemplate.Triggers>
                                            <DataTrigger Binding="{Binding Ativo}" Value="True">
                                                <Setter TargetName="StatusBadge" Property="Background" Value="#16A34A"/>
                                                <Setter TargetName="StatusText" Property="Text" Value="ATIVO"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Ativo}" Value="False">
                                                <Setter TargetName="StatusBadge" Property="Background" Value="#DC2626"/>
                                                <Setter TargetName="StatusText" Property="Text" Value="INATIVO"/>
                                            </DataTrigger>
                                        </DataTemplate.Triggers>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="AÇÕES" Width="130">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Click="AlternarStatus_Click" Background="Transparent" BorderThickness="0" Cursor="Hand">
                                            <Border Background="#1E293B" CornerRadius="8" Padding="10,5">
                                                <TextBlock x:Name="BtnText" Foreground="White" FontSize="11"/>
                                            </Border>
                                        </Button>
                                        <DataTemplate.Triggers>
                                            <DataTrigger Binding="{Binding Ativo}" Value="True">
                                                <Setter TargetName="BtnText" Property="Text" Value="DESATIVAR"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Ativo}" Value="False">
                                                <Setter TargetName="BtnText" Property="Text" Value="ATIVAR"/>
                                            </DataTrigger>
                                        </DataTemplate.Triggers>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>