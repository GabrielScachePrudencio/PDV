<UserControl x:Class="PDV_LANCHES.Views.ViewsAdministrativo.AllRelatorio"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="750" d:DesignWidth="1100"
             Background="Transparent">

    <UserControl.Resources>
        <Style TargetType="DataGridCell">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="#0F172A"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#1E293B"/>
            <Setter Property="Height" Value="50"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Relatório Geral de Vendas" FontSize="26" FontWeight="Bold" Foreground="White"/>
            <UniformGrid Columns="3" Height="100" Margin="0,15,0,0">
                <Border Background="#0F172A" CornerRadius="12" BorderBrush="#1E293B" BorderThickness="1" Margin="0,0,12,0">
                    <StackPanel VerticalAlignment="Center" Margin="20,0">
                        <TextBlock Text="TOTAL VENDIDO" Foreground="#94A3B8" FontSize="11" FontWeight="Bold"/>
                        <TextBlock x:Name="lblTotalVendido" Text="R$ 0,00" Foreground="#10B981" FontSize="24" FontWeight="Black"/>
                    </StackPanel>
                </Border>
                <Border Background="#0F172A" CornerRadius="12" BorderBrush="#1E293B" BorderThickness="1" Margin="0,0,12,0">
                    <StackPanel VerticalAlignment="Center" Margin="20,0">
                        <TextBlock Text="PEDIDOS" Foreground="#94A3B8" FontSize="11" FontWeight="Bold"/>
                        <TextBlock x:Name="lblTotalPedidos" Text="0" Foreground="White" FontSize="24" FontWeight="Black"/>
                    </StackPanel>
                </Border>
                <Border Background="#450A0A" CornerRadius="12" BorderBrush="#7F1D1D" BorderThickness="1">
                    <StackPanel VerticalAlignment="Center" Margin="20,0">
                        <TextBlock Text="CANCELADOS" Foreground="#F87171" FontSize="11" FontWeight="Bold"/>
                        <TextBlock x:Name="lblTotalCancelado" Text="R$ 0,00" Foreground="#F87171" FontSize="24" FontWeight="Black"/>
                    </StackPanel>
                </Border>
            </UniformGrid>
        </StackPanel>

        <Border Grid.Row="1" Background="#0F172A" CornerRadius="12" Padding="16" Margin="0,0,0,20" BorderBrush="#1E293B" BorderThickness="1">
            <StackPanel>
                <WrapPanel Margin="0,0,0,12">
                    <StackPanel Margin="0,0,12,0">
                        <TextBlock Text="Cliente (Nome ou CPF)" Foreground="#64748B" FontSize="11" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtBuscaCliente" Width="250" Height="35" VerticalContentAlignment="Center" Padding="8,0" Background="#1E293B" Foreground="White" BorderBrush="#334155"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,12,0">
                        <TextBlock Text="Vendedor" Foreground="#64748B" FontSize="11" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtBuscaVendedor" Width="180" Height="35" VerticalContentAlignment="Center" Padding="8,0" Background="#1E293B" Foreground="White" BorderBrush="#334155"/>
                    </StackPanel>
                </WrapPanel>

                <WrapPanel>
                    <StackPanel Margin="0,0,12,0">
                        <TextBlock Text="Início" Foreground="#64748B" FontSize="11" Margin="0,0,0,5"/>
                        <DatePicker x:Name="dtInicio" Width="135" Height="35"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,12,0">
                        <TextBlock Text="Fim" Foreground="#64748B" FontSize="11" Margin="0,0,0,5"/>
                        <DatePicker x:Name="dtFim" Width="135" Height="35"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,12,0">
                        <TextBlock Text="Pagamento" Foreground="#64748B" FontSize="11" Margin="0,0,0,5"/>
                        <ComboBox x:Name="comboFormaDePagamento" Width="160" Height="35" DisplayMemberPath="Descricao" SelectedValuePath="Id"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,12,0">
                        <TextBlock Text="Status" Foreground="#64748B" FontSize="11" Margin="0,0,0,5"/>
                        <ComboBox x:Name="comboStatus" Width="160" Height="35" DisplayMemberPath="nome" SelectedValuePath="id"/>
                    </StackPanel>

                    <Button Click="Pesquisar_Click" Content="🔍 Filtrar" Background="#3B82F6" Foreground="White" Width="100" Height="35" Margin="10,18,0,0" Cursor="Hand"/>
                    <Button Click="LimparFiltros_Click" Content="Limpar" Background="#475569" Foreground="White" Width="80" Height="35" Margin="8,18,0,0" Cursor="Hand"/>
                    <Button Click="Imprimir_Click" Content="🖨️" Background="#1E293B" Foreground="White" Width="45" Height="35" Margin="8,18,0,0" Cursor="Hand"/>
                </WrapPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" CornerRadius="12" ClipToBounds="True" BorderBrush="#1E293B" BorderThickness="1">
            <DataGrid x:Name="dgRelatorios" AutoGenerateColumns="False" CanUserAddRows="False" 
                      IsReadOnly="True" Background="#020617" HeadersVisibility="Column"
                      BorderThickness="0" MouseDoubleClick="VerDetalhes_Click">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="Data" Binding="{Binding DataCriacao, StringFormat='dd/MM/yy HH:mm'}" Width="130"/>
                    <DataGridTextColumn Header="Cliente" Binding="{Binding NomeCliente}" Width="*"/>
                    <DataGridTextColumn Header="CPF" Binding="{Binding CpfCliente}" Width="110"/>
                    <DataGridTextColumn Header="Vendedor" Binding="{Binding NomeUsuario}" Width="100"/>
                    <DataGridTextColumn Header="Pagamento" Binding="{Binding FormaPagamento}" Width="110"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding StatusPedido}" Width="100"/>
                    <DataGridTextColumn Header="Total" Binding="{Binding ValorTotal, StringFormat='C2'}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>