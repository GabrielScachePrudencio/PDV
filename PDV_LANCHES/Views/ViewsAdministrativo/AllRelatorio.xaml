<UserControl x:Class="PDV_LANCHES.Views.ViewsAdministrativo.AllRelatorio"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.expression/blend/2008" 
             mc:Ignorable="d"
             Focusable="True"
             KeyDown="UserControl_KeyDown"
             Background="#020617"
             d:DesignHeight="750" d:DesignWidth="1100">

    <UserControl.Resources>
        <Style x:Key="HighlightCard" TargetType="Border">
            <Setter Property="Background" Value="#0F172A"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#1E293B"/>
            <Setter Property="Padding" Value="20,15"/>
            <Setter Property="Margin" Value="5"/>
        </Style>

        <Style x:Key="StatusCard" TargetType="Border">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="4"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#3B82F6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="RowHeight" Value="45"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="Foreground" Value="#94A3B8"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="BorderBrush" Value="#3B82F6"/>
            <Setter Property="Height" Value="40"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="#0F172A"/>
            <Setter Property="Margin" Value="0,2"/>
            <Style.Triggers>
                <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                    <Setter Property="Background" Value="#161E2F"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2D3748"/>
                    <Setter Property="Cursor" Value="Hand"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#3B82F6"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Painel Administrativo de Vendas" FontSize="26" FontWeight="Bold" Foreground="White" Margin="5,0,0,20"/>

        <UniformGrid Grid.Row="1" Columns="3" Margin="0,0,0,10">
            <Border Style="{StaticResource HighlightCard}" BorderBrush="#10B981">
                <StackPanel>
                    <TextBlock Text="FATURAMENTO GERAL" Foreground="#86EFAC" FontSize="12" FontWeight="Bold" Margin="0,0,0,5"/>
                    <TextBlock x:Name="lblFaturadoGeral" Text="R$ 0,00" Foreground="#10B981" FontSize="32" FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource HighlightCard}" BorderBrush="#6366F1">
                <StackPanel>
                    <TextBlock Text="LUCRO LÍQUIDO" Foreground="#C7D2FE" FontSize="12" FontWeight="Bold" Margin="0,0,0,5"/>
                    <TextBlock x:Name="lblLucroGeral" Text="R$ 0,00" Foreground="#818CF8" FontSize="32" FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource HighlightCard}" BorderBrush="#F43F5E">
                <StackPanel>
                    <TextBlock Text="TOTAL DE PEDIDOS" Foreground="#FDA4AF" FontSize="12" FontWeight="Bold" Margin="0,0,0,5"/>
                    <TextBlock x:Name="lblTotalPedidos" Text="0" Foreground="White" FontSize="32" FontWeight="Bold"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <UniformGrid Grid.Row="2" Columns="5" Margin="0,0,0,20">

            <Border Style="{StaticResource StatusCard}" Background="#450A0A" BorderBrush="#7F1D1D" BorderThickness="1">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="CANCELADOS" Foreground="#FCA5A5" FontSize="10" FontWeight="Bold"/>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom">
                        <TextBlock x:Name="lblTotalCancelado" Text="R$ 0,00" Foreground="#F87171" FontSize="18" FontWeight="Bold"/>
                        <TextBlock x:Name="lblQtdeCancelado" Text="(0)" Foreground="#FCA5A5" FontSize="14" Margin="8,0,0,2" VerticalAlignment="Bottom"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource StatusCard}" Background="#022C22" BorderBrush="#134E4A" BorderThickness="1">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="PRONTOS" Foreground="#99F6E4" FontSize="10" FontWeight="Bold"/>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom">
                        <TextBlock x:Name="lblTotalProntos" Text="R$ 0,00" Foreground="#2DD4BF" FontSize="18" FontWeight="Bold"/>
                        <TextBlock x:Name="lblQtdeProntos" Text="(0)" Foreground="#99F6E4" FontSize="14" Margin="8,0,0,2" VerticalAlignment="Bottom"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource StatusCard}" Background="#082F49" BorderBrush="#075985" BorderThickness="1">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="FINALIZADOS" Foreground="#BAE6FD" FontSize="10" FontWeight="Bold"/>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom">
                        <TextBlock x:Name="lblTotalFinalizados" Text="R$ 0,00" Foreground="#38BDF8" FontSize="18" FontWeight="Bold"/>
                        <TextBlock x:Name="lblQtdeFinalizados" Text="(0)" Foreground="#BAE6FD" FontSize="14" Margin="8,0,0,2" VerticalAlignment="Bottom"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource StatusCard}" Background="#3F1D0B" BorderBrush="#7C2D12" BorderThickness="1">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="ESTORNADOS" Foreground="#FFEDD5" FontSize="10" FontWeight="Bold"/>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom">
                        <TextBlock x:Name="lblTotalEstornados" Text="R$ 0,00" Foreground="#FB923C" FontSize="18" FontWeight="Bold"/>
                        <TextBlock x:Name="lblQtdeEstornados" Text="(0)" Foreground="#FFEDD5" FontSize="14" Margin="8,0,0,2" VerticalAlignment="Bottom"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource StatusCard}" Background="#0F172A" BorderBrush="#334155" BorderThickness="1">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="VENDIDO HOJE" Foreground="#94A3B8" FontSize="10" FontWeight="Bold"/>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom">
                        <TextBlock x:Name="lblTotalVendido" Text="R$ 0,00" Foreground="#22C55E" FontSize="18" FontWeight="Bold"/>
                        <TextBlock x:Name="lblVendasHoje" Text="(0)" Foreground="#94A3B8" FontSize="14" Margin="8,0,0,2" VerticalAlignment="Bottom"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </UniformGrid>

        <Border Grid.Row="3" Background="#0F172A" CornerRadius="10" Padding="15" Margin="0,0,0,20" BorderBrush="#1E293B" BorderThickness="1">
            <WrapPanel>
                <StackPanel Margin="0,0,10,0">
                    <TextBlock Text="Pesquisar Cliente/CPF" Foreground="#64748B" FontSize="10" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtBuscaCliente" Width="180" Height="32" Background="#1E293B" Foreground="White" BorderThickness="1" BorderBrush="#334155" Padding="8,4"/>
                </StackPanel>
                <StackPanel Margin="0,0,10,0">
                    <TextBlock Text="Vendedor" Foreground="#64748B" FontSize="10" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtBuscaVendedor" Width="130" Height="32" Background="#1E293B" Foreground="White" BorderThickness="1" BorderBrush="#334155" Padding="8,4"/>
                </StackPanel>
                <StackPanel Margin="0,0,10,0">
                    <TextBlock Text="Data Início" Foreground="#64748B" FontSize="10" Margin="0,0,0,5"/>
                    <DatePicker x:Name="dtInicio" Width="110" Height="32"/>
                </StackPanel>
                <StackPanel Margin="0,0,10,0">
                    <TextBlock Text="Data Fim" Foreground="#64748B" FontSize="10" Margin="0,0,0,5"/>
                    <DatePicker x:Name="dtFim" Width="110" Height="32"/>
                </StackPanel>
                <StackPanel Margin="0,0,10,0">
                    <TextBlock Text="Pagamento" Foreground="#64748B" FontSize="10" Margin="0,0,0,5"/>
                    <ComboBox x:Name="comboFormaDePagamento" Width="130" Height="32" DisplayMemberPath="Descricao" SelectedValuePath="Id"/>
                </StackPanel>
                <StackPanel Margin="0,0,10,0">
                    <TextBlock Text="Status" Foreground="#64748B" FontSize="10" Margin="0,0,0,5"/>
                    <ComboBox x:Name="comboStatus" Width="130" Height="32" DisplayMemberPath="nome" SelectedValuePath="id"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" Margin="5,0,0,0">
                    <Button Content="Filtrar" Width="80" Height="32" Click="Pesquisar_Click" Background="#2563EB"/>
                    <Button Content="Limpar" Width="70" Height="32" Margin="10,0" Click="LimparFiltros_Click" Background="#475569"/>
                    <Button Content="🖨️" Width="40" Height="32" Click="Imprimir_Click" Background="#059669"/>
                </StackPanel>
            </WrapPanel>
        </Border>

        <Border Grid.Row="4" CornerRadius="10" BorderBrush="#1E293B" BorderThickness="1" ClipToBounds="True">
            <DataGrid x:Name="dgRelatorios" AutoGenerateColumns="False" IsReadOnly="True" Background="#020617" BorderThickness="0" MouseDoubleClick="VerDetalhes_Click">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="DATA" Binding="{Binding DataCriacao, StringFormat='dd/MM/yyyy'}" Width="100"/>
                    <DataGridTextColumn Header="HORA" Binding="{Binding DataCriacao, StringFormat='HH:mm'}" Width="80"/>
                    <DataGridTextColumn Header="CLIENTE" Binding="{Binding NomeCliente}" Width="*"/>
                    <DataGridTextColumn Header="CPF" Binding="{Binding CpfCliente}" Width="120"/>
                    <DataGridTextColumn Header="STATUS" Binding="{Binding StatusPedido}" Width="110"/>
                    <DataGridTextColumn Header="TOTAL" Binding="{Binding ValorTotal, StringFormat='C2'}" Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Foreground" Value="#10B981"/>
                                <Setter Property="TextAlignment" Value="Right"/>
                                <Setter Property="Margin" Value="0,0,10,0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>