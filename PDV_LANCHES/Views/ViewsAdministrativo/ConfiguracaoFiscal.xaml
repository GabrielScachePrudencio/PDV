<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:av="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="av" x:Class="PDV_LANCHES.Views.ViewsAdministrativo.ConfiguracaoFiscal"
             Background="Transparent" av:DesignWidth="436.779">

    <UserControl.Resources>
        <Style x:Key="FieldLabel" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="#94A3B8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,15,0,5"/>
        </Style>

        <Style TargetType="{x:Type TextBox}">
            <Setter Property="Background" Value="#020617"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#1E293B"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="CaretBrush" Value="#3B82F6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border x:Name="border" CornerRadius="8" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#3B82F6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ComboBox}">
            <Setter Property="Background" Value="#020617"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderBrush" Value="#1E293B"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="40">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel MaxWidth="600" HorizontalAlignment="Left">

                <StackPanel Margin="0,0,0,30">
                    <TextBlock Text="Configurações Fiscais" FontSize="28" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="Gerencie as informações tributárias e de emissão da sua loja." 
                               Foreground="#64748B" FontSize="14" Margin="0,5,0,0"/>
                </StackPanel>

                <Border Background="#1E293B" Height="1" Margin="0,0,0,10" Opacity="0.3"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="CNPJ" Style="{StaticResource FieldLabel}"/>
                        <TextBox Tag="00.000.000/0000-00"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Inscrição Estadual" Style="{StaticResource FieldLabel}"/>
                        <TextBox/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="0">
                        <TextBlock Text="Regime Tributário" Style="{StaticResource FieldLabel}"/>
                        <ComboBox>
                            <ComboBoxItem Content="Simples Nacional" IsSelected="True"/>
                            <ComboBoxItem Content="Lucro Presumido"/>
                            <ComboBoxItem Content="Lucro Real"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="2">
                        <TextBlock Text="Ambiente de Emissão" Style="{StaticResource FieldLabel}"/>
                        <ComboBox>
                            <ComboBoxItem Content="Homologação (Teste)" IsSelected="True"/>
                            <ComboBoxItem Content="Produção (Real)"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Grid.Column="0">
                        <TextBlock Text="Série da NF" Style="{StaticResource FieldLabel}"/>
                        <TextBox Text="1"/>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Grid.Column="2">
                        <TextBlock Text="Número Inicial" Style="{StaticResource FieldLabel}"/>
                        <TextBox Text="1"/>
                    </StackPanel>

                    <StackPanel Grid.Row="3" Grid.Column="0">
                        <TextBlock Text="Alíquota Padrão (%)" Style="{StaticResource FieldLabel}"/>
                        <TextBox Text="0.00"/>
                    </StackPanel>
                </Grid>

                <Button Content="Salvar Alterações Fiscais" 
                        Margin="0,40,0,0" 
                        Height="45" 
                        Width="220"
                        HorizontalAlignment="Left"
                        Background="#3B82F6" 
                        Foreground="White" 
                        FontWeight="Bold" 
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>