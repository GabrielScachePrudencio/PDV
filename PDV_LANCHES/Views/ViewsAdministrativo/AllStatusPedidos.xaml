<UserControl x:Class="PDV_LANCHES.Views.ViewsAdministrativo.AllStatusPedidos"
          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:PDV_LANCHES.Views.ViewsAdministrativo"
    mc:Ignorable="d"
    d:DesignHeight="600" d:DesignWidth="1000"
         HorizontalAlignment="Stretch" 
         VerticalAlignment="Stretch">
    <Grid Margin="24">
        <Border Background="#0F172A" CornerRadius="16" BorderBrush="#1E293B" BorderThickness="1" Padding="24">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock Text="Status de Pedidos" FontSize="24" FontWeight="Bold" Foreground="White"/>
                </StackPanel>

                <Border Grid.Row="1" Background="#020617" CornerRadius="12" Padding="20" Margin="0,0,0,25" BorderBrush="#1E293B" BorderThickness="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="200"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,20,0">
                            <TextBlock Text="NOME DO NOVO STATUS" Foreground="#64748B" FontSize="10" FontWeight="Bold" Margin="0,0,0,8"/>
                            <TextBox x:Name="txtNomeCategoria" Height="45" Padding="12" Background="#0F172A" Foreground="White" BorderBrush="#1E293B" VerticalContentAlignment="Center">
                                <TextBox.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="8"/>
                                    </Style>
                                </TextBox.Resources>
                            </TextBox>
                        </StackPanel>

                        <Button Grid.Column="1" Content="ADICIONAR" Height="45" VerticalAlignment="Bottom" Background="#3B82F6" Foreground="White" FontWeight="Bold" Click="NovaCategoria_Click">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="8"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </Grid>
                </Border>

                <Border Grid.Row="2" CornerRadius="12" ClipToBounds="True" BorderBrush="#1E293B" BorderThickness="1">
                    <DataGrid x:Name="dgCategorias" Background="#020617" AutoGenerateColumns="False" CanUserAddRows="False" RowHeight="60" BorderThickness="0" HeadersVisibility="Column" GridLinesVisibility="None">
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="#020617"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#0F172A"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>

                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#1E293B"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Height" Value="45"/>
                                <Setter Property="Padding" Value="15,0"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding id}" Width="80" IsReadOnly="True"/>

                            <DataGridTextColumn Header="Nome" Binding="{Binding nome}" Width="*" IsReadOnly="True"/>

                            <DataGridTemplateColumn Header="Status" Width="150">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border x:Name="StatusBadge" CornerRadius="10" Padding="8,2" HorizontalAlignment="Left">
                                            <TextBlock x:Name="StatusText" FontSize="10" FontWeight="Bold" Foreground="White"/>
                                        </Border>
                                        <DataTemplate.Triggers>
                                            <DataTrigger Binding="{Binding ativo}" Value="True">
                                                <Setter TargetName="StatusBadge" Property="Background" Value="#16A34A"/>
                                                <Setter TargetName="StatusText" Property="Text" Value="ATIVO"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding ativo}" Value="False">
                                                <Setter TargetName="StatusBadge" Property="Background" Value="#DC2626"/>
                                                <Setter TargetName="StatusText" Property="Text" Value="INATIVO"/>
                                            </DataTrigger>
                                        </DataTemplate.Triggers>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Ações" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Click="AlternarStatus_Click" Background="Transparent" BorderThickness="0" Cursor="Hand">
                                            <Border Background="#1E293B" CornerRadius="8" Padding="10,5">
                                                <TextBlock x:Name="BtnText" Foreground="White" FontSize="11"/>
                                            </Border>
                                        </Button>
                                        <DataTemplate.Triggers>
                                            <DataTrigger Binding="{Binding ativo}" Value="True">
                                                <Setter TargetName="BtnText" Property="Text" Value="DESATIVAR"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding ativo}" Value="False">
                                                <Setter TargetName="BtnText" Property="Text" Value="ATIVAR"/>
                                            </DataTrigger>
                                        </DataTemplate.Triggers>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>

                    </DataGrid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
