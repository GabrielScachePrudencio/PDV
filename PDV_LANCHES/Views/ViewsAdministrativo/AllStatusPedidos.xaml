<UserControl x:Class="PDV_LANCHES.Views.ViewsAdministrativo.AllStatusPedidos"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:PDV_LANCHES.Views.ViewsAdministrativo"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000"
             HorizontalAlignment="Stretch" 
             VerticalAlignment="Stretch">

    <Grid Margin="24">
        <Border Background="#FFFFFF" CornerRadius="16" BorderBrush="#E2E8F0" BorderThickness="1" Padding="24">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock Text="Gerenciar Status de Pedidos" FontSize="24" FontWeight="Bold" Foreground="#0F172A"/>
                    <TextBlock Text="Cadastre e controle os estados dos pedidos no sistema." Foreground="#64748B" FontSize="13"/>
                </StackPanel>

                <Border Grid.Row="1" Background="#F8FAFC" CornerRadius="12" Padding="20" Margin="0,0,0,25" BorderBrush="#E2E8F0" BorderThickness="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="200"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,20,0">
                            <TextBlock Text="NOME DO NOVO STATUS" Foreground="#64748B" FontSize="10" FontWeight="Bold" Margin="0,0,0,8"/>
                            <TextBox x:Name="txtNomeCategoria" Height="45" Padding="12" Background="White" Foreground="#0F172A" BorderBrush="#CBD5E1" VerticalContentAlignment="Center">
                                <TextBox.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="8"/>
                                    </Style>
                                </TextBox.Resources>
                            </TextBox>
                        </StackPanel>

                        <Button Grid.Column="1" Content="ADICIONAR STATUS" Height="45" VerticalAlignment="Bottom" Background="#3B82F6" Foreground="White" FontWeight="Bold" Click="NovaCategoria_Click">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="8"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </Grid>
                </Border>

                <Border Grid.Row="2" CornerRadius="12" ClipToBounds="True" BorderBrush="#E2E8F0" BorderThickness="1">
                    <DataGrid x:Name="dgCategorias" Background="#FFFFFF" AutoGenerateColumns="False" CanUserAddRows="False" RowHeight="60" BorderThickness="0" HeadersVisibility="Column" GridLinesVisibility="None">

                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="White"/>
                                <Setter Property="Foreground" Value="#0F172A"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F1F5F9"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>

                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#F1F5F9"/>
                                <Setter Property="Foreground" Value="#0F172A"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Height" Value="45"/>
                                <Setter Property="Padding" Value="15,0"/>
                                <Setter Property="BorderBrush" Value="#E2E8F0"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding id}" Width="80" IsReadOnly="True"/>

                            <DataGridTextColumn Header="NOME DO STATUS" Binding="{Binding nome}" Width="*" IsReadOnly="True"/>

                            <DataGridTemplateColumn Header="SITUAÇÃO" Width="150">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border x:Name="StatusBadge" CornerRadius="12" Padding="10,4" HorizontalAlignment="Left" VerticalAlignment="Center">
                                            <TextBlock x:Name="StatusText" FontSize="11" FontWeight="Bold" Foreground="White"/>
                                        </Border>
                                        <DataTemplate.Triggers>
                                            <DataTrigger Binding="{Binding ativo}" Value="True">
                                                <Setter TargetName="StatusBadge" Property="Background" Value="#10B981"/>
                                                <Setter TargetName="StatusText" Property="Text" Value="ATIVO"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding ativo}" Value="False">
                                                <Setter TargetName="StatusBadge" Property="Background" Value="#94A3B8"/>
                                                <Setter TargetName="StatusText" Property="Text" Value="INATIVO"/>
                                            </DataTrigger>
                                        </DataTemplate.Triggers>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="AÇÕES" Width="150">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Click="AlternarStatus_Click" Background="Transparent" BorderThickness="0" Cursor="Hand">
                                            <Border x:Name="ActionBorder" Background="#F1F5F9" CornerRadius="8" Padding="12,6">
                                                <TextBlock x:Name="BtnText" Foreground="#475569" FontSize="11" FontWeight="Bold"/>
                                            </Border>
                                        </Button>
                                        <DataTemplate.Triggers>
                                            <DataTrigger Binding="{Binding ativo}" Value="True">
                                                <Setter TargetName="BtnText" Property="Text" Value="DESATIVAR"/>
                                                <Setter TargetName="ActionBorder" Property="Background" Value="#FEF2F2"/>
                                                <Setter TargetName="BtnText" Property="Foreground" Value="#EF4444"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding ativo}" Value="False">
                                                <Setter TargetName="BtnText" Property="Text" Value="ATIVAR STATUS"/>
                                                <Setter TargetName="ActionBorder" Property="Background" Value="#F0FDFA"/>
                                                <Setter TargetName="BtnText" Property="Foreground" Value="#0D9488"/>
                                            </DataTrigger>
                                        </DataTemplate.Triggers>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>

                    </DataGrid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>