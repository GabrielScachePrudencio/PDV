<Window x:Class="PDV_LANCHES.Views.ViewsAdministrativo.NovoProduto"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cadastrar Produto" Height="680" Width="520"
        WindowStartupLocation="CenterScreen" Background="Transparent"
        AllowsTransparency="True" WindowStyle="None">

    <Window.Resources>
        <SolidColorBrush x:Key="BackgroundPrincipal" Color="#F8FAFC"/>
        <SolidColorBrush x:Key="BackgroundCard" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="BordaPrincipal" Color="#E2E8F0"/>
        <SolidColorBrush x:Key="TextoPrincipal" Color="#0F172A"/>
        <SolidColorBrush x:Key="TextoSecundario" Color="#475569"/>
        <SolidColorBrush x:Key="CorPrimaria" Color="#3B82F6"/>
        <SolidColorBrush x:Key="CorSucesso" Color="#22C55E"/>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#F1F5F9"/>
            <Setter Property="Foreground" Value="{StaticResource TextoPrincipal}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BordaPrincipal}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" 
                                          HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource CorPrimaria}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkComboBox" TargetType="ComboBox">
            <Setter Property="Foreground" Value="{StaticResource TextoPrincipal}"/>
            <Setter Property="Background" Value="#F1F5F9"/>
            <Setter Property="BorderBrush" Value="{StaticResource BordaPrincipal}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid x:Name="MainGrid" SnapsToDevicePixels="True">
                            <ToggleButton x:Name="ToggleButton" Grid.ColumnSpan="2"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Focusable="False" ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="templateRoot" Background="{TemplateBinding Background}" 
                                                BorderBrush="{TemplateBinding BorderBrush}" 
                                                BorderThickness="{TemplateBinding BorderThickness}" 
                                                CornerRadius="8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="30"/>
                                                </Grid.ColumnDefinitions>
                                                <Path x:Name="Arrow" Grid.Column="1" Data="M 0 0 L 4 4 L 8 0 Z" Fill="{StaticResource TextoSecundario}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" 
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              Margin="{TemplateBinding Padding}" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                            <Popup x:Name="PART_Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" PopupAnimation="Slide">
                                <Border x:Name="DropDownBorder" Background="White" BorderBrush="{StaticResource BordaPrincipal}" BorderThickness="1" CornerRadius="8" MinWidth="{Binding ActualWidth, ElementName=MainGrid}" Margin="0,4,0,0">
                                    <ScrollViewer MaxHeight="300">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernPrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource CorPrimaria}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border BorderBrush="{StaticResource BordaPrincipal}" BorderThickness="1" CornerRadius="20" Background="{StaticResource BackgroundCard}">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,25">
                <TextBlock Text="NOVO PRODUTO" Foreground="{StaticResource TextoPrincipal}" FontSize="20" FontWeight="ExtraBold"/>
                <Border Height="3" Width="40" Background="{StaticResource CorPrimaria}" HorizontalAlignment="Left" Margin="0,5,0,0"/>
            </StackPanel>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="NOME DO PRODUTO" Foreground="{StaticResource TextoSecundario}" FontSize="11" FontWeight="Bold" Margin="0,0,0,8"/>
                    <TextBox x:Name="txtNome" Height="45" Margin="0,0,0,20" Style="{StaticResource ModernTextBox}"/>

                    <TextBlock Text="CATEGORIA" Foreground="{StaticResource TextoSecundario}" FontSize="11" FontWeight="Bold" Margin="0,0,0,8"/>
                    <ComboBox x:Name="comboCategoria" Height="45" Margin="0,0,0,20" 
                              DisplayMemberPath="nome" SelectedValuePath="id"
                              Style="{StaticResource DarkComboBox}"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="PREÇO (R$)" Foreground="{StaticResource TextoSecundario}" FontSize="11" FontWeight="Bold" Margin="0,0,0,8"/>
                            <TextBox x:Name="txtValor" Height="45" Margin="0,0,0,20" Style="{StaticResource ModernTextBox}"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="0,0,10,0">
                            <TextBlock Text="UNITÁRIO (R$)" Foreground="{StaticResource TextoSecundario}" FontSize="11" FontWeight="Bold" Margin="0,0,0,8" />
                            <TextBox x:Name="txtPrecoUnitario" Height="45" Style="{StaticResource ModernTextBox}"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2" Margin="5,0,0,0">
                            <TextBlock Text="QTD INICIAL" Foreground="{StaticResource TextoSecundario}" FontSize="11" FontWeight="Bold" Margin="0,0,0,8"/>
                            <TextBox x:Name="txtQuantidade" Height="45" Margin="0,0,0,20" 
                                     Style="{StaticResource ModernTextBox}" 
                                     Text="0"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="IMAGEM DO PRODUTO" Foreground="{StaticResource TextoSecundario}" FontSize="11" FontWeight="Bold" Margin="0,0,0,8"/>
                    <Border BorderBrush="{StaticResource BordaPrincipal}" BorderThickness="2" CornerRadius="10" Height="120" Background="#F1F5F9" Cursor="Hand" MouseLeftButtonDown="SelecionarImagem_Click">
                        <Grid>
                            <Image x:Name="imgPreview" Stretch="Uniform" Margin="10"/>
                            <StackPanel x:Name="pnlPlaceholder" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock Text="📷" FontSize="24" HorizontalAlignment="Center"/>
                                <TextBlock Text="Selecionar" Foreground="{StaticResource TextoSecundario}" FontSize="10" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <StackPanel Margin="0,15,0,0">
                        <TextBlock Text="STATUS" Foreground="{StaticResource TextoSecundario}" FontSize="11" FontWeight="Bold" Margin="0,0,0,8"/>
                        <CheckBox x:Name="chkDisponivel" Content="Disponível" Foreground="{StaticResource TextoPrincipal}" 
                                 IsChecked="True" VerticalContentAlignment="Center" Height="45"/>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="CANCELAR" Click="Fechar_Click" Background="Transparent" Foreground="{StaticResource TextoSecundario}" Margin="0,0,20,0" BorderThickness="0" Cursor="Hand"/>
                <Button Content="SALVAR PRODUTO" Click="Salvar_Click" Width="180" Height="45" Style="{StaticResource ModernPrimaryButton}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>