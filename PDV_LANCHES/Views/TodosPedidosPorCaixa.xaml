<Window x:Class="PDV_LANCHES.Views.TodosPedidosPorCaixa"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pedidos do Caixa" Height="550" Width="800"
        WindowStartupLocation="CenterScreen" WindowStyle="None" 
        AllowsTransparency="True" Background="Transparent">

    <Window.Resources>
        <!-- ========================================= -->
        <!-- PALETA DE CORES - MODO CLARO -->
        <!-- ========================================= -->

        <SolidColorBrush x:Key="BackgroundPrincipal" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="BackgroundSecundario" Color="#F8FAFC"/>
        <SolidColorBrush x:Key="BordaPrincipal" Color="#E2E8F0"/>
        <SolidColorBrush x:Key="BordaSecundaria" Color="#CBD5E1"/>

        <SolidColorBrush x:Key="TextoPrincipal" Color="#0F172A"/>
        <SolidColorBrush x:Key="TextoSecundario" Color="#475569"/>
        <SolidColorBrush x:Key="TextoTerciario" Color="#64748B"/>

        <SolidColorBrush x:Key="CorPrimaria" Color="#3B82F6"/>
        <SolidColorBrush x:Key="CorSucesso" Color="#10B981"/>

        <SolidColorBrush x:Key="HoverBackground" Color="#F1F5F9"/>

    </Window.Resources>

    <Border Background="{StaticResource BackgroundPrincipal}" CornerRadius="20" BorderBrush="{StaticResource BordaPrincipal}" BorderThickness="1">
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Button Content="Voltar" HorizontalAlignment="Right" VerticalAlignment="Top" 
                 Margin="0,7,10,0" Width="64" Height="30"
                 Background="Transparent" Foreground="{StaticResource TextoPrincipal}" 
                 FontSize="16" BorderThickness="0" Cursor="Hand" 
                    Click="Fechar_Click"/>

            <StackPanel Grid.Row="0" Margin="0,0,0,15">
                <TextBlock Text="PEDIDOS DO CAIXA" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextoPrincipal}" />
                <Border Height="2" Background="{StaticResource CorSucesso}" HorizontalAlignment="Left" Width="30" Margin="0,8,0,0"/>
            </StackPanel>

            <Border Grid.Row="1" Background="{StaticResource BackgroundSecundario}" CornerRadius="12" Padding="15" Margin="0,0,0,15" BorderBrush="{StaticResource BordaPrincipal}" BorderThickness="1">
                <UniformGrid Columns="2">
                    <StackPanel>
                        <TextBlock Text="CAIXA" FontSize="10" Foreground="{StaticResource TextoTerciario}" FontWeight="Bold"/>
                        <TextBlock x:Name="txtIdCaixa" Text="00" FontSize="16" Foreground="{StaticResource TextoPrincipal}" FontWeight="Bold"/>
                    </StackPanel>
                    <StackPanel HorizontalAlignment="Right">
                        <TextBlock Text="TOTAL" FontSize="10" Foreground="{StaticResource TextoTerciario}" FontWeight="Bold" HorizontalAlignment="Right"/>
                        <TextBlock x:Name="txtTotalVendas" Text="R$ 0,00" FontSize="16" Foreground="{StaticResource CorSucesso}" FontWeight="Bold"/>
                    </StackPanel>
                </UniformGrid>
            </Border>

            <DataGrid Grid.Row="2" x:Name="dgPedidos" Background="Transparent" AutoGenerateColumns="False" 
                      CanUserAddRows="False" RowHeight="45" BorderThickness="0" 
                      HeadersVisibility="Column" GridLinesVisibility="None"
                      Foreground="{StaticResource TextoSecundario}">

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{StaticResource TextoTerciario}"/>
                        <Setter Property="Padding" Value="10,0,10,10"/>
                        <Setter Property="FontSize" Value="11"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="{StaticResource BordaPrincipal}"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource HoverBackground}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nº" Binding="{Binding Id}" Width="60"/>
                    <DataGridTextColumn Header="HORÁRIO" Binding="{Binding DataCriacao, StringFormat={}{0:HH:mm}}" Width="100"/>
                    <DataGridTextColumn Header="CLIENTE" Binding="{Binding NomeCliente}" Width="*"/>
                    <DataGridTextColumn Header="PAGAMENTO" Binding="{Binding FormaPagamento}" Width="120"/>

                    <DataGridTextColumn Header="VALOR" Binding="{Binding ValorTotal, StringFormat=C2}" Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Foreground" Value="{StaticResource CorSucesso}"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Margin" Value="10,0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTemplateColumn Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="detalhes →" 
                                        Background="Transparent" Foreground="{StaticResource CorPrimaria}" 
                                        FontSize="12" BorderThickness="0" Cursor="Hand"
                                        Click="VerDetalhesPedido_Click">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <TextBlock Text="{TemplateBinding Content}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Border>
</Window>