<Window x:Class="PDV_LANCHES.Views.TodasVendasCompleto"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        WindowState="Maximized" 
        WindowStyle="SingleBorderWindow"
        Title="Todas as Vendas" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen" Background="#020617">

    <Window.Resources>
        <Style x:Key="ModernDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowBackground" Value="#0F172A"/>
            <Setter Property="AlternatingRowBackground" Value="#1E293B"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#334155"/>
            <Setter Property="RowHeight" Value="50"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="ColumnHeaderStyle">
                <Setter.Value>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#1E293B"/>
                        <Setter Property="Foreground" Value="#94A3B8"/>
                        <Setter Property="Padding" Value="10"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#334155"/>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ModernCalendar" TargetType="{x:Type Calendar}">
            <Setter Property="Background" Value="#0F172A" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#334155" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Calendar}">
                        <StackPanel x:Name="PART_Root" HorizontalAlignment="Center">
                            <CalendarItem x:Name="PART_CalendarItem" 
                                  Background="{TemplateBinding Background}" 
                                  BorderBrush="{TemplateBinding BorderBrush}" 
                                  BorderThickness="1" 
                                  Style="{TemplateBinding CalendarItemStyle}" />
                        </StackPanel>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernDatePicker" TargetType="{x:Type DatePicker}">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="#1E293B" />
            <Setter Property="BorderBrush" Value="#334155" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="5" />
            <Setter Property="CalendarStyle" Value="{StaticResource ModernCalendar}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DatePicker}">
                        <Grid x:Name="PART_Root">
                            <Border x:Name="Bg" 
                            Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}" 
                            CornerRadius="8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <DatePickerTextBox x:Name="PART_TextBox" 
                                               Grid.Column="0" 
                                               Foreground="{TemplateBinding Foreground}" 
                                               Background="Transparent" 
                                               BorderThickness="0" 
                                               VerticalContentAlignment="Center"
                                               Padding="10,0"/>

                                    <Button x:Name="PART_Button" 
                                    Grid.Column="1" 
                                    Content="📅" 
                                    Foreground="#22C55E"
                                    Padding="10,0"
                                    Background="Transparent" 
                                    BorderThickness="0"
                                    Cursor="Hand"/>
                                </Grid>
                            </Border>

                            <Popup x:Name="PART_Popup" 
                           AllowsTransparency="True" 
                           Placement="Bottom" 
                           StaysOpen="False" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DarkComboBox" TargetType="ComboBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid x:Name="MainGrid" SnapsToDevicePixels="True">
                            <ToggleButton x:Name="ToggleButton" Grid.ColumnSpan="2"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Focusable="False" ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="templateRoot" Background="{TemplateBinding Background}" 
                                                BorderBrush="{TemplateBinding BorderBrush}" 
                                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="30"/>
                                                </Grid.ColumnDefinitions>
                                                <Path x:Name="Arrow" Grid.Column="1" Data="M 0 0 L 4 4 L 8 0 Z" Fill="#94A3B8"
                                                      HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              Margin="{TemplateBinding Padding}" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                            <Popup x:Name="PART_Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" PopupAnimation="Slide">
                                <Border x:Name="DropDownBorder" Background="#0F172A" BorderBrush="#334155" BorderThickness="1" CornerRadius="8"
                                        MinWidth="{Binding ActualWidth, ElementName=MainGrid}" Margin="0,4,0,0">
                                    <ScrollViewer MaxHeight="300" SnapsToDevicePixels="True">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#94A3B8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Margin" Value="12,4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="20,0,0,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1E293B"/>
                    <Setter Property="Foreground" Value="#22C55E"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#0F172A" BorderBrush="#1E293B" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Margin="0,40,0,30">
                    <Border Width="90" Height="90" CornerRadius="45" BorderBrush="#22C55E" BorderThickness="2" Padding="4" HorizontalAlignment="Center">
                        <Grid>
                            <Ellipse x:Name="CircleMask" Fill="#020617"/>

                            <Border CornerRadius="45" ClipToBounds="True">
                                <Grid>
                                    <Image x:Name="imgLogoMenu" Stretch="UniformToFill">
                                        <Image.OpacityMask>
                                            <VisualBrush Visual="{Binding ElementName=CircleMask}"/>
                                        </Image.OpacityMask>
                                    </Image>

                                    <TextBlock x:Name="txtLogoPlaceholder" 
                Text="🍴" 
                FontSize="35" 
                Foreground="#334155" 
                HorizontalAlignment="Center" 
                VerticalAlignment="Center" 
                Visibility="Collapsed"/>
                                </Grid>
                            </Border>
                        </Grid>
                    </Border>

                    <TextBlock x:Name="txtNomeLoja" Text="CARREGANDO..." Foreground="White" FontSize="15" FontWeight="Black" Margin="15,15,15,0" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"/>

                    <Border Background="#064E3B" CornerRadius="4" Padding="8,2" HorizontalAlignment="Center" Margin="0,8,0,0">
                        <TextBlock Text="OPERACIONAL" Foreground="#34D399" FontSize="9" FontWeight="Black" />
                    </Border>
                </StackPanel>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="20,10">
                        <Button Style="{StaticResource MenuButtonStyle}" x:Name="novoPedido" 
                                Content="➕ Novo Pedido" Click="novoPedido_Click" 
                                Foreground="White" Background="#166534" Margin="0,0,0,20"/>

                        <TextBlock Text="DATA INÍCIO" FontSize="10" FontWeight="Bold" Foreground="#475569" Margin="0,0,0,8"/>
                        <DatePicker x:Name="dataInicio" 
            Style="{StaticResource ModernDatePicker}" 
            Height="40" 
            Margin="0,0,0,15"/>

                        <TextBlock Text="DATA FIM" FontSize="10" FontWeight="Bold" Foreground="#475569" Margin="0,0,0,8"/>
                        <DatePicker x:Name="dataFim" 
            Style="{StaticResource ModernDatePicker}" 
            Height="40" 
            Margin="0,0,0,20"/>

                        <TextBlock Text="STATUS" FontSize="10" FontWeight="Bold" Foreground="#475569" Margin="0,0,0,8"/>
                        <ComboBox x:Name="comboStatusFiltro" Style="{StaticResource DarkComboBox}" 
                                  Margin="0,0,0,20" SelectionChanged="ComboBox_SelectionChanged"/>

                        <TextBlock Text="BUSCAR CLIENTE (CPF/NOME)" FontSize="10" FontWeight="Bold" Foreground="#475569" Margin="0,0,0,8"/>
                        <TextBox x:Name="txtBuscaCliente" Height="40" Background="#1E293B" Foreground="White" 
                                 BorderThickness="1" BorderBrush="#334155" Padding="10,5" VerticalContentAlignment="Center" 
                                 KeyUp="txtBuscaCliente_KeyUp" Margin="0,0,0,20"/>
                        
                        <TextBlock Text="BUSCAR COLABORADOR " FontSize="10" FontWeight="Bold" Foreground="#475569" Margin="0,0,0,8"/>
                        <TextBox x:Name="txtBuscaColaborador" Height="40" Background="#1E293B" Foreground="White" 
                                 BorderThickness="1" BorderBrush="#334155" Padding="10,5" VerticalContentAlignment="Center" 
                                 KeyUp="txtBuscaCliente_KeyUp"/>
                    </StackPanel>
                </ScrollViewer>

                <StackPanel Grid.Row="2" Margin="15,20">
                    <Button Content="Voltar ao Início" Background="#1E293B" Foreground="White" Height="40" Margin="0,5" Click="VoltarPedidos_Click">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="10"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Column="1" Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#0F172A" CornerRadius="16" Padding="25,15" BorderBrush="#1E293B" BorderThickness="1" Margin="0,0,0,20">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Relatório de Vendas" FontSize="18" FontWeight="SemiBold" Foreground="White"/>
                        <TextBlock Text="•" Foreground="#334155" Margin="12,0" VerticalAlignment="Center"/>
                        <TextBlock x:Name="usuarioLogadoHome" Text="Operador" Foreground="#94A3B8" VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Text="2026" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="#64748B"/>
                </Grid>
            </Border>

            <Border Grid.Row="1" Background="#0F172A" CornerRadius="16" Padding="10" BorderBrush="#1E293B" BorderThickness="1">
                <DataGrid x:Name="dgVendas" Style="{StaticResource ModernDataGrid}" SelectionChanged="dgVendas_SelectionChanged" MouseDoubleClick="VerDetalhes_click">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>

                        <DataGridTemplateColumn Header="USUÁRIO" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding NomeUsuario}" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="CPF CLIENTE" Binding="{Binding CpfCliente}" Width="140"/>

                        <DataGridTemplateColumn Header="STATUS" Width="130">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="#064E3B" CornerRadius="12" Padding="8,2" HorizontalAlignment="Left">
                                        <TextBlock Text="{Binding StatusPedido}" Foreground="#34D399" FontSize="11" FontWeight="Bold"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="DATA" Binding="{Binding DataCriacao, StringFormat='dd/MM/yyyy HH:mm'}" Width="140"/>

                        <DataGridTextColumn Header="PAGAMENTO" Binding="{Binding FormaPagamento}" Width="120"/>

                        <DataGridTemplateColumn Header="VALOR" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding ValorTotal, StringFormat='C2'}" Foreground="#22C55E" FontWeight="Bold" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                       
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
    </Grid>
</Window>