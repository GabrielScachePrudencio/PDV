<Window x:Class="PDV_LANCHES.Views.TodasVendasCompleto"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowState="Maximized" Background="#020617" Title="Histórico de Vendas" 
        Height="800" Width="1400">

    <Window.Resources>
        <Style x:Key="DarkComboBox" TargetType="ComboBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid x:Name="MainGrid" SnapsToDevicePixels="True">
                            <ToggleButton x:Name="ToggleButton" Grid.ColumnSpan="2"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Focusable="False" ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="templateRoot" Background="{TemplateBinding Background}" 
                                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="30"/>
                                                </Grid.ColumnDefinitions>
                                                <Path x:Name="Arrow" Grid.Column="1" Data="M 0 0 L 4 4 L 8 0 Z" Fill="#94A3B8" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}"
                                              Margin="{TemplateBinding Padding}" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                            <Popup x:Name="PART_Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" PopupAnimation="Slide">
                                <Border x:Name="DropDownBorder" Background="#0F172A" BorderBrush="#334155" BorderThickness="1" CornerRadius="8"
                                        MinWidth="{Binding ActualWidth, ElementName=MainGrid}" Margin="0,4,0,0">
                                    <ScrollViewer MaxHeight="300">
                                        <ItemsPresenter />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernPrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#10B981"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Padding" Value="25,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#059669"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernDatePicker" TargetType="{x:Type DatePicker}">
            <Setter Property="Background" Value="#1E293B" />
            <Setter Property="Height" Value="40"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DatePicker}">
                        <Border Background="{TemplateBinding Background}" BorderBrush="#334155" BorderThickness="1" CornerRadius="8">
                            <Grid x:Name="PART_Root">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="40" />
                                </Grid.ColumnDefinitions>
                                <DatePickerTextBox x:Name="PART_TextBox" Grid.Column="0" Foreground="White" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="10,0"/>
                                <Button x:Name="PART_Button" Grid.Column="1" Content="📅" Foreground="#3B82F6" Background="Transparent" BorderThickness="0" Cursor="Hand"/>
                                <Popup x:Name="PART_Popup" AllowsTransparency="True" Placement="Bottom" StaysOpen="False" PopupAnimation="Fade"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,25">
            <Button Click="Voltar_Click" HorizontalAlignment="Left" Background="Transparent" BorderThickness="0" Cursor="Hand">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="←" Foreground="#3B82F6" FontSize="24" FontWeight="Bold"/>
                    <TextBlock Text="VOLTAR PARA HOME" Foreground="#94A3B8" FontSize="12" FontWeight="Bold" VerticalAlignment="Center" Margin="10,0,0,0"/>
                </StackPanel>
            </Button>
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="TODAS AS VENDAS" Foreground="White" FontSize="22" FontWeight="ExtraBold"/>
                <Border Height="3" Width="50" Background="#3B82F6" HorizontalAlignment="Left" Margin="2,4,0,0" CornerRadius="2"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Background="#0F172A" CornerRadius="16" Padding="20" Margin="0,0,0,20" BorderBrush="#1E293B" BorderThickness="1">
            <Grid>
                <WrapPanel Orientation="Horizontal" VerticalAlignment="Bottom">
                    <StackPanel Margin="0,0,15,0">
                        <TextBlock Text="CLIENTE" FontSize="10" FontWeight="Bold" Foreground="#475569" Margin="0,0,0,8"/>
                        <TextBox x:Name="txtBuscaCliente" Width="180" Height="40" Style="{StaticResource ModernTextBox}"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,15,0">
                        <TextBlock Text="STATUS" FontSize="10" FontWeight="Bold" Foreground="#475569" Margin="0,0,0,8"/>
                        <ComboBox Style="{StaticResource DarkComboBox}" x:Name="comboStatus" Width="120"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,15,0">
                        <TextBlock Text="PAGAMENTO" FontSize="10" FontWeight="Bold" Foreground="#475569" Margin="0,0,0,8"/>
                        <ComboBox Style="{StaticResource DarkComboBox}" x:Name="comboFormaDePagamento" Width="130"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,15,0">
                        <TextBlock Text="DATA INÍCIO" FontSize="10" FontWeight="Bold" Foreground="#475569" Margin="0,0,0,8"/>
                        <DatePicker x:Name="dtInicio" Width="140" Style="{StaticResource ModernDatePicker}"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,15,0">
                        <TextBlock Text="DATA FIM" FontSize="10" FontWeight="Bold" Foreground="#475569" Margin="0,0,0,8"/>
                        <DatePicker x:Name="dtFim" Width="140" Style="{StaticResource ModernDatePicker}"/>
                    </StackPanel>
                    <Button Click="Pesquisar_Click" Width="100" Height="40" Margin="5,0,0,0" Background="#3B82F6" Foreground="White" FontWeight="Bold" VerticalAlignment="Bottom">
                        <TextBlock Text="🔍 FILTRAR"/>
                    </Button>
                </WrapPanel>
                <Button x:Name="novoPedido" Width="200"  Style="{StaticResource ModernPrimaryButton}" Click="novoPedido_Click" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                    <StackPanel Orientation="Horizontal" >
                        <TextBlock Text="➕" FontSize="16" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <TextBlock Text="NOVO PEDIDO" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <Border Grid.Row="2" Padding="20,10" Margin="0,0,0,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="130"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="ID" Foreground="#475569" FontWeight="Bold" Grid.Column="0"/>
                <TextBlock Text="DATA E HORA" Foreground="#475569" FontWeight="Bold" Grid.Column="1"/>
                <TextBlock Text="STATUS" Foreground="#475569" FontWeight="Bold" Grid.Column="2"/>
                <TextBlock Text="CLIENTE" Foreground="#475569" FontWeight="Bold" Grid.Column="3"/>
                <TextBlock Text="CPF" Foreground="#475569" FontWeight="Bold" Grid.Column="4"/>
                <TextBlock Text="VENDEDOR" Foreground="#475569" FontWeight="Bold" Grid.Column="5"/>
                <TextBlock Text="FORMA PGTO" Foreground="#475569" FontWeight="Bold" Grid.Column="6"/>
                <TextBlock Text="TOTAL" Foreground="#475569" FontWeight="Bold" Grid.Column="7" HorizontalAlignment="Right"/>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto" MouseDoubleClick="VerDetalhes_PreviewMouseDown">
            <ItemsControl x:Name="ListaVendas">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,0,8" Background="#0F172A" CornerRadius="12" Padding="20" BorderBrush="#1E293B" BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="140"/>
                                    <ColumnDefinition Width="140"/>
                                    <ColumnDefinition Width="130"/>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="140"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="{Binding Id, StringFormat='#{0}'}" Foreground="White" FontWeight="Bold" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="1" Text="{Binding DataCriacao, StringFormat='dd/MM/yy HH:mm'}" Foreground="#94A3B8" VerticalAlignment="Center"/>

                                <Border Grid.Column="2" Background="#1E293B" HorizontalAlignment="Left" CornerRadius="6" Padding="10,4" VerticalAlignment="Center" BorderBrush="#334155" BorderThickness="1">
                                    <TextBlock Text="{Binding StatusPedido}" Foreground="#3B82F6" FontSize="11" FontWeight="Bold"/>
                                </Border>

                                <TextBlock Grid.Column="3" Text="{Binding NomeCliente}" Foreground="White" FontWeight="SemiBold" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Grid.Column="4" Text="{Binding CpfCliente}" Foreground="#475569" FontSize="12" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="5" Text="{Binding NomeUsuario}" Foreground="#94A3B8" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="6" Text="{Binding FormaPagamento}" Foreground="#94A3B8" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="7" Text="{Binding ValorTotal, StringFormat='R$ {0:N2}'}" Foreground="#22C55E" FontSize="16" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>