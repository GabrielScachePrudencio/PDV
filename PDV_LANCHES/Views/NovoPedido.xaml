<Window x:Class="PDV_LANCHES.Views.NovoPedido"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowState="Maximized" WindowStyle="SingleBorderWindow"
        Title="Novo Pedido" Height="650" Width="1000"
        WindowStartupLocation="CenterScreen" Background="#0f172a">
    <Window.Resources>
        <Style x:Key="DarkComboBox" TargetType="ComboBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid x:Name="MainGrid" SnapsToDevicePixels="True">
                            <ToggleButton x:Name="ToggleButton"
                              Grid.ColumnSpan="2"
                              Background="{TemplateBinding Background}"
                              BorderBrush="{TemplateBinding BorderBrush}"
                              BorderThickness="{TemplateBinding BorderThickness}"
                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                              Focusable="False"
                              ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="templateRoot" 
                                    Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                    CornerRadius="8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="30"/>
                                                </Grid.ColumnDefinitions>
                                                <Path x:Name="Arrow" 
                                          Grid.Column="1"
                                          Data="M 0 0 L 4 4 L 8 0 Z"
                                          Fill="#94A3B8"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <ContentPresenter x:Name="ContentSite"
                                  IsHitTestVisible="False"
                                  Content="{TemplateBinding SelectionBoxItem}"
                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                  ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                  Margin="{TemplateBinding Padding}"
                                  VerticalAlignment="Center"
                                  HorizontalAlignment="Left"/>

                            <Popup x:Name="PART_Popup"
                       Placement="Bottom"
                       IsOpen="{TemplateBinding IsDropDownOpen}"
                       AllowsTransparency="True"
                       Focusable="False"
                       PopupAnimation="Slide">
                                <Border x:Name="DropDownBorder"
                            Background="#0F172A" 
                            BorderBrush="#334155"
                            BorderThickness="1"
                            CornerRadius="8"
                            MinWidth="{Binding ActualWidth, ElementName=MainGrid}"
                            Margin="0,4,0,0">
                                    <ScrollViewer MaxHeight="300" SnapsToDevicePixels="True">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="false">
                                <Setter TargetName="DropDownBorder" Property="Height" Value="95"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="BorderBrush" Value="#22C55E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="110"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10,0,10,25">
            <Button x:Name="btnVoltar" Content="" FontFamily="Segoe MDL2 Assets" 
                    FontSize="18" Width="45" Height="45" Background="#1e293b" Foreground="White" 
                    BorderThickness="1" BorderBrush="#334155" Cursor="Hand" Click="btnVoltar_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="22"/>
                    </Style>
                </Button.Resources>
            </Button>
            <TextBlock Text="Finalizar Novo Pedido" FontSize="28" FontWeight="ExtraBold" Foreground="White" VerticalAlignment="Center" Margin="15,0,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Background="#1e293b" CornerRadius="12" Padding="20" Margin="10,0,10,20" BorderBrush="#334155" BorderThickness="1">
            <StackPanel Orientation="Horizontal">
                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="CPF DO CLIENTE" FontSize="10" FontWeight="Bold" Foreground="#94a3b8" Margin="0,0,0,8"/>
                    <TextBox x:Name="inputCpfCliente" 
                     Width="200" Height="45" Padding="12,0" FontSize="15" 
                     MaxLength="14"
                     TextChanged="inputCpfCliente_TextChanged"
                     VerticalContentAlignment="Center" Background="#0f172a" Foreground="White" 
                     BorderThickness="1" BorderBrush="#475569" CaretBrush="White"/>
                </StackPanel>

                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="NOME DO CLIENTE" FontSize="10" FontWeight="Bold" Foreground="#94a3b8" Margin="0,0,0,8"/>
                    <TextBox x:Name="inputNomeCliente" Text="" Width="350" Height="45" Padding="12,0" FontSize="15" 
                             VerticalContentAlignment="Center" Background="#0f172a" Foreground="White" 
                             BorderThickness="1" BorderBrush="#475569" CaretBrush="White" />
                </StackPanel>

                <TextBlock x:Name="LabelCPF" VerticalAlignment="Bottom" Margin="0,0,0,10" FontSize="18" FontWeight="Bold" Foreground="#60a5fa"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2" Margin="10,0">
            <ScrollViewer x:Name="scrollCardapio" VerticalScrollBarVisibility="Auto" Visibility="Visible">
                <ItemsControl x:Name="ListaCardapioItems">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Width="230" Margin="0,0,15,15" Background="#1e293b" CornerRadius="12" Padding="15" BorderBrush="#334155" BorderThickness="1">
                                <StackPanel>
                                    <Border CornerRadius="10" ClipToBounds="True" Margin="0,0,0,10">
                                        <Image Source="{Binding pathImg}" Width="200" Height="120" Stretch="UniformToFill"/>
                                    </Border>
                                    <TextBlock Text="{Binding Categoria}" FontSize="10" Foreground="#94a3b8" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding Nome}" FontSize="16" FontWeight="Bold" Foreground="White" Margin="0,5"/>
                                    <TextBlock Text="{Binding Valor, StringFormat='R$ {0:F2}'}" FontSize="18" Foreground="#60a5fa" FontWeight="Bold"/>
                                    <Grid Margin="0,15,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="65"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox Text="{Binding QuantidadeSelecionada, UpdateSourceTrigger=PropertyChanged}" 
                                                 Height="38" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" 
                                                 Background="#0f172a" Foreground="White" BorderThickness="1" BorderBrush="#475569"/>
                                        <Button Grid.Column="1" Content="ADICIONAR" Height="38" Margin="8,0,0,0" 
                                                Background="#3b82f6" Foreground="White" FontWeight="Bold" BorderThickness="0" 
                                                Cursor="Hand" Click="AdicionarItem_Click" Tag="{Binding}">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="8"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <Border x:Name="borderResumo" Visibility="Collapsed" Background="#1e293b" CornerRadius="15" Padding="30" BorderBrush="#334155" BorderThickness="1">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <StackPanel x:Name="stackResumoItens"/>

                        <StackPanel Margin="0,20,0,0">
                            <TextBlock Text="FORMA DE PAGAMENTO" FontSize="10" FontWeight="Bold" Foreground="#94a3b8" Margin="0,0,0,8"/>
                            <ComboBox x:Name="comboPagamento" 
                                      Style="{StaticResource DarkComboBox}"
                                      Background="#0f172a" Foreground="White" 
                                      BorderBrush="#475569" BorderThickness="1" 
                                      Height="45" DisplayMemberPath="Descricao" SelectedValuePath="Id">
                                <ComboBox.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <VirtualizingStackPanel Background="#0f172a"/>
                                    </ItemsPanelTemplate>
                                </ComboBox.ItemsPanel>
                                <ComboBox.ItemContainerStyle>
                                    <Style TargetType="ComboBoxItem">
                                        <Setter Property="Background" Value="#1e293b"/>
                                        <Setter Property="Foreground" Value="White"/>
                                        <Setter Property="Padding" Value="10"/>
                                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                        <Setter Property="BorderBrush" Value="#334155"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#3b82f6"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </ComboBox.ItemContainerStyle>
                            </ComboBox>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <Border Grid.Row="3" Background="#1e293b" CornerRadius="15" Margin="10,15,10,10" Padding="30,0" BorderBrush="#334155" BorderThickness="1">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Total do Pedido:" FontSize="16" Foreground="#94a3b8" VerticalAlignment="Center"/>
                    <TextBlock x:Name="txtTotalDisplay" Text="R$ 0,00" FontSize="32" FontWeight="Bold" Foreground="#22c55e" Margin="15,0,0,0"/>
                </StackPanel>

                <Button x:Name="buttonProximo" 
                        Content="PRÓXIMO ➔" 
                        HorizontalAlignment="Right" VerticalAlignment="Center" 
                        Width="220" Height="55" 
                        Background="#22c55e" Foreground="White" 
                        FontSize="16" FontWeight="ExtraBold" 
                        BorderThickness="0" Cursor="Hand" Click="Button_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="12"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <Button x:Name="buttonSairSemDarBaixa"  Visibility="Hidden"
                        Content="Dar baixa depois" 
                        HorizontalAlignment="Left" VerticalAlignment="Center" 
                        Width="220" Height="55" 
                        Foreground="Black" 
                        FontSize="16" FontWeight="ExtraBold" 
                        BorderThickness="0" Cursor="Hand" Click="ButtonSairSemDarBaixa_Click" Margin="412,0,0,0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="12"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Window>