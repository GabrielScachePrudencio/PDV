<Window x:Class="PDV_LANCHES.Views.PedidoInfo"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Editar Pedido" Height="650" Width="500"
        WindowStartupLocation="CenterScreen" Background="Transparent"
        WindowStyle="None" AllowsTransparency="True">
    <Window.Resources>
        <Style x:Key="DarkComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid x:Name="MainGrid">
                            <ToggleButton x:Name="ToggleButton"
                                      Background="{TemplateBinding Background}"
                                      BorderBrush="{TemplateBinding BorderBrush}"
                                      BorderThickness="{TemplateBinding BorderThickness}"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="templateRoot" 
                                            Background="{TemplateBinding Background}" 
                                            BorderBrush="{TemplateBinding BorderBrush}" 
                                            BorderThickness="{TemplateBinding BorderThickness}" 
                                            CornerRadius="8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="30"/>
                                                </Grid.ColumnDefinitions>
                                                <Path x:Name="Arrow" Grid.Column="1" Data="M 0 0 L 4 4 L 8 0 Z" Fill="#94A3B8" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" Margin="{TemplateBinding Padding}" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                            <Popup x:Name="PART_Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" PopupAnimation="Slide">
                                <Border x:Name="DropDownBorder" Background="#0F172A" BorderBrush="#334155" BorderThickness="1" CornerRadius="8" Margin="0,4,0,0">
                                    <ScrollViewer MaxHeight="300">
                                        <ItemsPresenter />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#0F172A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1E293B"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Border BorderBrush="#1E293B" BorderThickness="1" CornerRadius="24" Background="#020617">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,25">
                <TextBlock Text="DETALHES DO PEDIDO" FontSize="11" FontWeight="Black" Foreground="#22C55E"/>
                <TextBlock Text="Edição de Informações" FontSize="20" FontWeight="SemiBold" Foreground="White" Margin="0,5,0,0"/>
                <Separator Margin="0,15,0,0" Background="#1E293B"/>
            </StackPanel>

            <Grid Grid.Row="1" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,10,15">
                    <TextBlock Text="CPF DO CLIENTE" Foreground="#64748B" FontSize="10" FontWeight="Bold" Margin="0,0,0,8"/>
                    <TextBox x:Name="pedidoCpfInfo" Height="40" Padding="12,8" Background="#0F172A" BorderBrush="#1E293B" 
                             BorderThickness="1" Foreground="White" VerticalContentAlignment="Center" CaretBrush="#22C55E">
                        <TextBox.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </TextBox.Resources>
                    </TextBox>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,0,15">
                    <TextBlock Text="STATUS" Foreground="#64748B" FontSize="10" FontWeight="Bold" Margin="0,0,0,8"/>
                    <ComboBox 
                        Style="{StaticResource DarkComboBox}" x:Name="ComboBoxStatusPedido" Height="40" Background="#0F172A" Foreground="White" BorderBrush="#1E293B" SelectionChanged="ComboBox_SelectionChanged">
                    </ComboBox>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.ColumnSpan="2">
                    <Button x:Name="btnCardapioCompleto" Content="+ Adicionar Itens do Cardápio" Height="40" 
                            Background="#1E293B" Foreground="#94A3B8" BorderThickness="0" Click="CardapioCompleto_click" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Grid>

            <Border Grid.Row="2" Background="#0F172A" CornerRadius="16" Padding="10" BorderBrush="#1E293B" BorderThickness="1">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="itensPedidoStackPanel"/>
                </ScrollViewer>
            </Border>

            <Border Grid.Row="3" Margin="0,20,0,20" Padding="20,15" Background="#1E293B" CornerRadius="16">
                <DockPanel>
                    <TextBlock Text="TOTAL DO PEDIDO" Foreground="#94A3B8" VerticalAlignment="Center" FontWeight="Bold" FontSize="12"/>
                    <TextBlock x:Name="pedidoTotalInfo" Text="R$ 0,00" HorizontalAlignment="Right" Foreground="#22C55E" FontSize="24" FontWeight="Black"/>
                </DockPanel>
            </Border>

            <UniformGrid Grid.Row="4" Columns="2">
                <Button Content="CANCELAR" Height="50" Margin="0,0,8,0" Background="Transparent" Foreground="#64748B" 
                        FontWeight="Bold" BorderBrush="#1E293B" BorderThickness="1" Click="Fechar_Click" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="12"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <Button x:Name="btnSalvar" Content="SALVAR ALTERAÇÕES" Height="50" Background="#22C55E" Foreground="#064E3B" 
                        FontWeight="Black" BorderThickness="0" Click="Salvar_Click" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="12"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </UniformGrid>
        </Grid>
    </Border>
</Window>